# Legacy Application Code

`src/matrix/application/legacy` is the only sanctioned legacy area in the application layer.

- Do not add new feature work here.
- Only compatibility and transition code belongs here.
- The target state is gradual deletion as callers move to the current feature and app-core structure.

